---
const { created, modified } = Astro.props;
const age = new Date(modified).getTime() - new Date(created).getTime();
const lastTended = Math.floor(
  (new Date() - new Date(modified)) / (1000 * 60 * 60 * 24)
);
---

<small class="text-center">
  Este 🌲 se plantó hace {age} días y fué regado hace {lastTended} días
</small>
