---
import { canonLinks } from '../constants/general.constants';
---

{
  canonLinks.map(link => (
    <a
      href={link.href}
      class="flex flex-col p-4 border-2 rounded-lg hover:bg-[var(--pico-color-2)] transition-colors w-48 h-24"
    >
      <div class="flex flex-col items-center justify-center h-full">
        <h3 class="font-bold text-center">{link.title}</h3>
        {'description' in link && link.description && (
          <p class="text-sm text-[var(--pico-color-7)]">{link.description}</p>
        )}
      </div>
    </a>
  ))
}
